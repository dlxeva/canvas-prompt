# 隐私与本地数据

简体中文 · [English](./PRIVACY.md)

Canvas Prompt 以项目本地存储为基础。把某一轮交给 Codex 是一次单独、明确的操作。

## 每轮会保存什么

导出一轮后，Canvas Prompt 会把本地数据写入：

```text
<当前项目>/.canvas-prompt/
```

根据本轮使用的能力，内容可能包括：

- Prompt Package JSON 与原始生命周期 Trace；
- 最终 PNG 快照和选定的状态帧；
- 原始录音；
- Process IR 与 Compact Package；
- 轮次元数据、本地工件路径和 Codex 交付回执；
- 用来跟踪交付状态的 Codex 任务与 Turn 标识。

你可以在应用内的本地档案中查看并永久删除一轮。Canvas Prompt 无法恢复已删除的本地轮次。

## 麦克风与语音转写

任何一轮开始时，Canvas Prompt 都会请求麦克风权限。拒绝授权或麦克风不可用时，画布事件仍会继续记录，本轮不会获得音频。

当前开发环境会把已经完成的音频片段发送到 `127.0.0.1:8080` 的本地转写服务。Canvas Prompt 本身不运营云端转写服务。本地服务不可用时，原始录音仍可能保存在项目内；带时间戳的语音证据会标记为不可用。

如果你自行配置远程或第三方 ASR，数据处理将遵循对应服务的隐私、保留和区域条款。录制他人或机密内容前，请先取得必要许可。

## 把一轮交给 Codex

选择“发送到 Codex”后，Canvas Prompt 会通过本机 Codex app server，把该轮不可变上下文提交到当前 Codex 任务。提交的消息可能引用编译后的上下文、最终快照和项目内工件路径。提交后的数据处理遵循用户 Codex 账户及所配置模型服务的隐私与保留条款。

Canvas Prompt 会记录当前任务是否接收并完成了这次 Turn。之后删除本地轮次，只会清理本地档案；已经提交到 Codex 任务的内容无法由 Canvas Prompt 撤回，也不会从任务历史中自动移除。

## 公开示例

请勿提交真实录音、转写、截图、白板、项目内 `.canvas-prompt/` 文件夹或任务绑定信息。公开示例必须是合成数据，或已经获得明确授权并完成去标识化。

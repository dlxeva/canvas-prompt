# 隐私与本地数据

简体中文 · [English](./PRIVACY.md)

Canvas Prompt 以一块设备本地的活动白板为基础。结束一轮会在本地编译并保存上下文；用户明确要求继续时，兼容宿主可通过本地 MCP 读取最近完成的不可变轮次。这一动作本身不会向可见聊天框插入消息，也不保证宿主自动回复。

## 每轮会保存什么

导出一轮后，Canvas Prompt 会把本地数据写入：

```text
~/.canvas-prompt/board/
```

根据本轮使用的能力，内容可能包括：

- Prompt Package JSON 与原始生命周期 Trace。体积较大的 Trace 和 Package 会按受限大小分段保存在本地，再由本机重组；
- 最终 PNG 快照和选定的状态帧；
- 原始录音；
- Process IR 与 Compact Package；
- 轮次元数据、本地工件路径，以及宿主支持时的交接回执；
- 若存在，则记录本轮发起项目路径作为来源信息，而非读取路由键。

交互审阅轮次还可能保存紧凑的 PDF/PPTX 审阅包、审阅 Brief、页面批注、带时间戳
的语音片段，以及已标记页面的 PNG 视觉证据。不可变审阅档案不会复制 PDF 或
PPTX 原文件字节。对于 PPTX，本地渲染出的 PDF 衍生文件只作为审阅底稿；过程包
会分别记录原件与衍生文件身份。

你可以在应用内的本地档案中查看并永久删除一轮。Canvas Prompt 无法恢复已删除的本地轮次。

## 麦克风与语音转写

任何一轮开始时，Canvas Prompt 都会请求麦克风权限。拒绝授权或麦克风不可用时，画布事件仍会继续记录，本轮不会获得音频。

Canvas Prompt 的受管运行时会把已完成的音频片段发送到 `127.0.0.1` 上的本地转写服务。具体端口由启动器选择，并写入运行时身份，不固定为 `8080`。Canvas Prompt 本身不运营云端转写服务。本地 ASR 未就绪时，用户可以明确选择纯视觉回合；保存的录音会如实标记为缺少带时间戳的转写证据。

原始录音、回放分段和内联图片字节属于本地档案工件，常规模型上下文不会携带这些内容。宿主默认读取紧凑的结构化上下文；只有其集成明确支持时，才会按需请求某个已归档工件。

交互审阅同样遵循渐进披露边界。宿主先读取紧凑的页码、批注和语音证据；只有
某页的紧凑证据不足时，才可凭不可变过程包和页面身份按需请求该页归档画面。
完整文档与原文件字节不会进入常规 MCP 审阅响应。

内置交互原型审阅只使用合成内容。事件、状态快照、文字反馈和提案保留在浏览器
内存中，直到用户明确丢弃或关闭页面；导出的 JSON 副本由用户自行管理。该预览
排除敏感输入值，不采集语音，不向网络外传，也不能打开任意网址或用户项目。

如果你自行配置远程或第三方 ASR，数据处理将遵循对应服务的隐私、保留和区域条款。录制他人或机密内容前，请先取得必要许可。

## 宿主读取与交接

在 Codex Desktop 中，受支持的路径是在希望继续的任务输入明确续接指令，再由 Canvas Prompt 的本地 MCP 读取单一活动白板最近完成的不可变轮次。它不承诺自动写入可见聊天、投递到某个特定任务，或自动获得回复。

如果宿主记录了交接回执，之后删除本地轮次也只会清理本地档案；用户或宿主已经分享给模型服务的数据，Canvas Prompt 无法撤回，也不会从服务方历史中自动移除。

## 公开示例

请勿提交真实录音、转写、截图、白板、`.canvas-prompt/` 档案或任务绑定信息。公开示例必须是合成数据，或已经获得明确授权并完成去标识化。
